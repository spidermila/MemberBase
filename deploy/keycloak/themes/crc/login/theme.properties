# MedCover look for Keycloak's login pages: Keycloak's own v2 theme plus a
# stylesheet. CSS only, no template overrides, so upgrades stay painless.
parent=keycloak.v2
import=common/keycloak
styles=css/styles.css css/crc.css
darkMode=true
locales=cs
